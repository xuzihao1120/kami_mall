该包实现的是一个简单的策略模式
策略模式：使用策略模式可以轻松干掉大量if/else，代码也更优雅，灵活性、可扩展性更高

例子为：某某公司是做支付的，根据不同的客户类型会有不同的支付方式和支付产品，比如：信用卡、本地支付，而本地支付在中国又有微信支付、支付宝、云闪付、等更多其他第三方支付公司，这时候策略模式就派上用场了。

简单理解：
    定义一个策略接口，这里是支付接口（Ipayment接口）。再定义多个实现类，如：微信、支付宝、云闪付等。这里的支付接口作为参数从外部参入进来。
    注：有些策略模式文章使用了枚举、HashMap 的方式来根据策略名称映射策略实现类 ，这样是没有问题，但在使用了 Spring 框架的项目还是有点多此一举，完全可以发挥 Spring 框架的优势，使用 Bean 名称就能找到对应的策略实现类了。


策略模式的应用：
    JDK的应用：
        1、线程池的构造函数中有一个参数为拒绝策略接口。拒绝策略接口的实现有很多，故作为参数由外部传入。这就是一个策略接口。
        2、Comparator接口也是一个策略接口。它作为定义排序规则的接口。具体使用哪种排序由开发人员决定。


